# Local convenience dependencies only. Production is defined by the pinned
# Docker base/Sapiens commit plus constraints.txt. Install a torch/torchvision
# pair appropriate for the local GPU first; Sapiens2 requires torch >= 2.7.
-c constraints.txt
decord==0.6.0
numpy>=1.24
requests>=2.31
huggingface_hub
transformers
timm
kornia

# serverless adapters
runpod>=1.6
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
